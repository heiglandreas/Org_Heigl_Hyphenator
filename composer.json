{
    "name" : "Org_Heigl/Hyphenator",
    "type" : "library",
    "description" : "Word-Hyphenation for PHP based on the TeX-Hyphenation algorithm",
    "keywords" : [ "hyphenation", "hyphenate"],
    "homepage" : "http://github.com/heiglandreas/Org_Heigl_Hyphenator",
    "license" : "MIT",
    "authors" : [
        {
            "name" : "Andreas Heigl",
            "email" : "andreas@heigl.org",
            "homepage" : "http://andreas.heigl.org",
            "role" : "Developer"
        }
    ],
    "require" : {
        "php" : "^5.6||^7.0",
        "ext-mbstring" : "*"
    },
    "require-dev" : {
        "mockery/mockery" : "^0.9",
        "phpunit/phpunit": "^4.8||^5.3"
    },
    "autoload" : {
        "psr-4" : {
            "Org\\Heigl\\Hyphenator\\" : "src"
        }
    },
    "autoload-dev" : {
        "psr-4" : {
            "Org\\Heigl\\HyphenatorTest\\" : "tests"
        }
    },
    "scripts": {
        "check": [
            "@cs-check",
            "@test"
        ],
        "upload-coverage": "coveralls -v",
        "cs-download" : [
            "curl -o phpcs -L `curl -s https://api.github.com/repos/squizlabs/PHP_CodeSniffer/releases | grep browser_download_url | grep phpcs.phar\\\" | head -n 1 | cut -d '\"' -f 4`",
            "curl -o phpcs.asc -L `curl -s https://api.github.com/repos/squizlabs/PHP_CodeSniffer/releases | grep browser_download_url | grep phpcs.phar.asc\\\" | head -n 1 | cut -d '\"' -f 4`",
            "chmod 755 phpcs"
        ],
        "cs-check": [
            "@cs-download",
            "./phpcs --version && ./phpcs"
        ],
        "cs-fix": [
            "@cs-download",
            "./phpcs"
        ],
        "test": "phpunit",
        "test-coverage": "phpunit --coverage-clover clover.xml"
    }
}
